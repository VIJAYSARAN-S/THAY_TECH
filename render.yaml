services:
  - type: web
    name: project-management-server
    env: node
    buildCommand: cd project-management-tool/server && npm install
    startCommand: cd project-management-tool/server && npm start
    envVars:
      - key: DATABASE_URL
        sync: false
